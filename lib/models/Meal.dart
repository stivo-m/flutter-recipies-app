class Meal {
  final String id, title, category, area, thumbnail, tags, video, imageSource;

  Meal(
    this.id,
    this.title,
    this.category,
    this.area,
    this.thumbnail,
    this.tags,
    this.video,
    this.imageSource,
  );
}
